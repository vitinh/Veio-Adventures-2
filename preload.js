// Preload script
// This can expose specific Node.js APIs to the renderer process
window.addEventListener('DOMContentLoaded', () => {
  // This is where we would expose APIs if needed
  console.log('DOM fully loaded and parsed');
});
